services:
  gemini-cli:
    build: .
    volumes:
      # 現在のフォルダをコンテナに繋ぐ
      - .:/app
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    working_dir: /app
    tty: true
    stdin_open: true